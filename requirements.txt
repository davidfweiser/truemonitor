# truemonitor.py + treuemonitor-text.py — TrueNAS WebSocket JSON-RPC 2.0 API
websocket-client>=1.6.0

# truemonitor.py + truemonclient.py — credential/broadcast encryption (Fernet + PBKDF2HMAC)
cryptography>=3.4
